[build]
  publish = "build"  # Specify the root directory since your index.html is at the root
  # command = "npm run build"  # Uncomment if you have a build step

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
